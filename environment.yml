name: stampy
channels:
  - defaults
dependencies:
  - pip
  - numpy
  - sqlite
  - pip:
    - black
    - discord-py
    - google-api-python-client
    - google-auth-oauthlib
    - python-dotenv
    - gitpython
    - pytest
